const StartOrgRegistrationSection = () => {
    return (
        <section>
            Welcome to the org registration flow.
            Click The Button below to continue
        </section>
    );
};

export default StartOrgRegistrationSection;